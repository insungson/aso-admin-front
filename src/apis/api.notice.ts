/**
 * @description 배포 버튼 클릭
 * @param params
 * @returns
 */
export const fetchPutNoticeDeploy = async (params) => {
  try {
    const response = await Promise.resolve(true);
    return response;
  } catch (error) {
    return false;
  }
};
